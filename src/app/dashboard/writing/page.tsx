"use client";

import { SpeechIcon } from "@/components/icons";
import { PlaceholderSection } from "../components/PlaceholderSection";

export default function WritingPage() {
  return (
    <PlaceholderSection
      icon={SpeechIcon}
      title="Writing"
      description="Get instant feedback on structure, tone, and grammar as you write."
    />
  );
}
