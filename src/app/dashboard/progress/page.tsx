"use client";

import { ChartIcon } from "@/components/icons";
import { PlaceholderSection } from "../components/PlaceholderSection";

export default function ProgressPage() {
  return (
    <PlaceholderSection
      icon={ChartIcon}
      title="Progress"
      description="Track fluency, accuracy, and streaks over time."
    />
  );
}
