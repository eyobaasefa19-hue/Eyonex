"use client";

import { BookIcon } from "@/components/icons";
import { PlaceholderSection } from "../components/PlaceholderSection";

export default function VocabularyPage() {
  return (
    <PlaceholderSection
      icon={BookIcon}
      title="Vocabulary"
      description="Build your word power with spaced-repetition sets."
    />
  );
}
