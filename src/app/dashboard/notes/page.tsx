"use client";

import { NoteIcon } from "@/components/icons";
import { PlaceholderSection } from "../components/PlaceholderSection";

export default function NotesPage() {
  return (
    <PlaceholderSection
      icon={NoteIcon}
      title="Notes"
      description="Keep your vocabulary, rules, and examples in one organized place."
    />
  );
}
